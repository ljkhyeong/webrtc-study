[Unit]
Description=%i의 치명적인 ROUND 운영 실패 기록

[Service]
Type=oneshot
NoNewPrivileges=true
PrivateTmp=true
ProtectSystem=strict
ProtectHome=true
CapabilityBoundingSet=
ExecStart=/usr/bin/logger --stderr --priority daemon.crit --tag round-ops ROUND 운영 실패: %i
