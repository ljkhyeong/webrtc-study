# ROUND 작업 지시

## 범위와 참고 자료

- 사용자의 현재 요청과 이미 합의한 범위를 따른다. 스킬의 일반 절차 때문에 작업을 넓히거나 이미 허용된 작업을 다시 확인하지 않는다.
- 방 생성·스터디 관리는 BATON, 입장 준비·통화는 ROUND가 담당한다. 독립 실행의 방 생성 화면은 개발·검증용이다.
- 필요한 내용만 [README](README.md), [아키텍처](docs/architecture.md), [서버 운영](apps/signaling/README.md)에서 찾아 읽는다.
- WebRTC 연결·미디어·채팅 동작에는 `round-webrtc-flows`, Java 시그널링·인증·세션 처리에는 `round-spring-signaling`을 사용한다. 단순 문구·스타일·문서 수정에는 이 스킬들을 적용하지 않는다.

## 코드 기준

- 메시지 형식은 `packages/protocol/src/types.ts`, `validation.ts`와 Java `ProtocolParser`를 확인한다. 형식·오류 코드가 바뀔 때만 양쪽 계약과 관련 처리를 함께 수정한다.
- 브라우저와 서버의 입력 검증은 각각 유지한다. 같은 처리 안의 중복 검사는 실패 시점과 동작이 같을 때만 제거한다.
- 버전·이벤트·설정값 목록을 스킬에 복제하지 않는다. 실제 코드와 설정을 기준으로 한다.

## 검증 선택

- 브라우저 테스트는 기본적으로 headless로 실행한다. 사용자가 화면 확인을 요청하지 않으면 브라우저 창을 띄우거나 포커스를 이동하지 않는다.
- 문구·스타일·문서: 차이와 형식을 확인한다. 기존 문구 검사는 필요할 때 수정하고, 배치가 바뀌면 해당 화면을 확인한다.
- 웹·RTC 코어·프로토콜 동작: 바뀐 동작을 다루는 기존 테스트와 해당 패키지 타입 검사를 실행한다.
- Java 동작: 관련 테스트를 실행한다. Spring 설정·빈 연결은 실제 컨텍스트에서, 빌드·패키징 변경은 서버 빌드로 확인한다.
- 입장·협상·채팅·미디어·재연결: 영향받는 두 참가자 브라우저 흐름을 확인한다.

명령은 `package.json`, `apps/web/package.json`과 Gradle 설정에서 확인한다.
`npm run check`는 여러 계층에 영향을 주는 공통 변경이나 CI에서 요구할 때 실행한다.
테스트는 새 동작이나 재발할 오류를 확인할 때만 추가한다. 필요한 검증이 통과하면 새 변경·실패·남은 우려 없이 검사를 확대하거나 반복하지 않는다.

## 2단계 피드백 루프

- 작업을 시작할 때 `git rev-parse HEAD`로 기준 커밋을 기록한다. 작업 중 커밋했더라도 종료 검사는 이 기준을 사용한다.
- 파일이나 함께 컴파일해야 하는 최소 묶음을 작성한 직후 `git diff -- <경로>`로 해당 변경을 읽고 지역 검사를 실행한다.
- 문서·설정·TypeScript는 해당 파일의 Prettier 검사부터 실행한다. TypeScript는 변경 패키지의 타입 검사와 가장 가까운 테스트를, Java는 변경한 소스 세트의 컴파일과 관련 테스트를 이어서 실행한다.
- 중간 상태가 컴파일되지 않는 연속 편집은 함께 컴파일되는 최소 묶음까지 작성한 뒤 검사한다. 전체 검사를 파일 저장마다 반복하지 않는다.
- 작업 종료 직전 `git status --short`, `git diff --check <기준 커밋> --`, `git diff <기준 커밋> --` 순서로 새 파일, 공백 오류, 전체 변경을 확인한다.
- `git diff`에 나타나지 않는 미추적 파일은 내용을 따로 읽거나 스테이징한 뒤 전체 차이에 포함한다.
- 전체 차이에서는 패키지 경계, 핸들러·서비스 책임, 공개 계약과 문서·테스트 누락을 확인한다. Java 소스나 빌드 설정을 바꿨으면 `npm run check:architecture`를 실행한다.
- 같은 리비전에서 `npm run check:java`가 통과했다면 포함된 아키텍처 검사를 다시 실행하지 않는다.

## 검증 기록과 재시도

- 검사 명령과 실패 시 다음 조치는 [검증 절차](docs/agent-validation.md)를 따른다. 작은 웹 수정은 `npm run check:web -- <테스트 파일>`로 확인한다.
- 이어서 작업할 때 `output/agent-validation.md`가 있으면 기준 커밋·변경 파일·검사 결과·남은 조건을 확인한다. 관련 코드·설정·실행 환경이 같으면 통과한 검사를 반복하지 않는다. CI 검사는 별도로 실행한다.
- 실패하면 코드·환경·명령 사용 오류를 구분한다. 원인이나 실행 조건을 바꾸기 전에는 같은 검사를 다시 실행하지 않는다. 필요한 권한은 도구의 승인 절차로 요청한다.
- 이미 읽은 스킬·문서는 변경되었거나 새 작업에 필요한 부분만 다시 읽는다. 경로는 현재 저장소에서 찾고, 세션 기록은 요약을 먼저 본 뒤 필요한 명령·실패 출력만 확인한다.
