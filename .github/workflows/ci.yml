name: CI

on:
  pull_request:
  push:
    branches:
      - main
      - 'codex/**'

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    name: 타입 검사, 테스트 및 빌드
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: 저장소 checkout
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          persist-credentials: false

      - name: Node.js 설정
        uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
        with:
          node-version: 22
          cache: npm

      - name: npm 11 설정
        run: npm install --global npm@11.7.0 --no-audit --no-fund

      - name: Java 설정
        uses: actions/setup-java@03ad4de0992f5dab5e18fcb136590ce7c4a0ac95 # v5
        with:
          distribution: temurin
          java-version: 21
          cache: gradle

      - name: 의존성 설치
        run: npm ci

      - name: 저장소 검사 실행
        run: npm run check

  browser-e2e:
    name: Chromium WebRTC 및 BATON edge smoke 테스트
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: 저장소 checkout
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          persist-credentials: false

      - name: Node.js 설정
        uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
        with:
          node-version: 22
          cache: npm

      - name: npm 11 설정
        run: npm install --global npm@11.7.0 --no-audit --no-fund

      - name: Java 설정
        uses: actions/setup-java@03ad4de0992f5dab5e18fcb136590ce7c4a0ac95 # v5
        with:
          distribution: temurin
          java-version: 21
          cache: gradle

      - name: 의존성 설치
        run: npm ci

      - name: Chromium 설치
        run: npx playwright install --with-deps chromium

      - name: Chromium 전체 미디어 smoke 테스트 실행
        run: npx playwright test --project=chromium-full-media --project=chromium-mobile-layout

      - name: BATON production edge 권한 부여 테스트 실행
        run: npm run test:e2e:baton-edge

      - name: 브라우저 진단 자료 업로드
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: playwright-diagnostics
          path: output/playwright/
          if-no-files-found: ignore
          retention-days: 7

  browser-webkit:
    name: WebKit 호환성 smoke 테스트
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: 저장소 checkout
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          persist-credentials: false

      - name: Node.js 설정
        uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
        with:
          node-version: 22
          cache: npm

      - name: npm 11 설정
        run: npm install --global npm@11.7.0 --no-audit --no-fund

      - name: Java 설정
        uses: actions/setup-java@03ad4de0992f5dab5e18fcb136590ce7c4a0ac95 # v5
        with:
          distribution: temurin
          java-version: 21
          cache: gradle

      - name: 의존성 설치
        run: npm ci

      - name: WebKit 설치
        run: npx playwright install --with-deps webkit

      - name: WebKit 호환성 smoke 테스트 실행
        run: npx playwright test --project=webkit-smoke --project=webkit-mobile-layout

      - name: WebKit 진단 자료 업로드
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: playwright-webkit-diagnostics
          path: output/playwright/
          if-no-files-found: ignore
          retention-days: 7

  deployment:
    name: 배포 artifact 및 이미지 검증
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: 저장소 checkout
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          persist-credentials: false

      - name: Node.js 설정
        uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
        with:
          node-version: 22
          cache: npm

      - name: npm 11 설정
        run: npm install --global npm@11.7.0 --no-audit --no-fund

      - name: 검증 의존성 설치
        run: npm ci

      - name: 배포 검증
        run: bash ops/ci/validate-deployment.sh
